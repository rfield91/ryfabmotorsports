# Copy this file to .env and fill in real values for local development.
# .env is gitignored — never commit real secrets/endpoints here.

# Formspree endpoint the contact form submits to.
# Leave unset to use the testing form (the default fallback in contact.astro).
FORMSPREE_ENDPOINT=https://formspree.io/f/xeaqrepr
